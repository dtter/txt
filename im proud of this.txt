#include <iostream>
using namespace std;

int main() {
    int x, y;
    cout << "insert the value of x: ";
    cin >> x;
    
    if (x == 69){
        cout << "not 69 bestie. next \n";
        cin >> y;
        
        if (y >= 2; y < 20) {
            cout << "ok better";
        }
        else {
            cout << "try later";
        }
    }
    else {
        cout << x << " is great i guess";
    }

    return 0;
}